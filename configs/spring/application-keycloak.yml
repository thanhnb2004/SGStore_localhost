keycloak:
  server-url: ${KC_SERVER_URL:http://192.168.21.141:8080}
  realm: ${KC_REALM}
  admin-realm: ${KC_ADMIN_REALM:master}
  admin-client-id: ${KC_CLIENT_ID:admin-cli}
  admin-username: ${KC_ADMIN_USERNAME:admin}
  admin-password: ${KC_ADMIN_PASSWORD:admin}